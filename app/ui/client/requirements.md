## Packages
framer-motion | For smooth message animations and UI transitions
lucide-react | Icon set (already in base but ensuring availability)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
External RAG API at http://127.0.0.1:8000
Requires X-API-Key header handling
Tailwind config needs to support 'Space Grotesk' font family
